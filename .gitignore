# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local machine overrides (paths, email, model choices) — see config/settings.json
config/settings.local.json

# Runtime state
*.log
models/

# Google OAuth client + token, and tracker backups — never commit these
config/gmail_credentials.json
config/gmail_token.json
*.jarvis-backup-*.xlsx
